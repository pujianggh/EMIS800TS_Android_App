<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/MatchMatch.Vertical"
    android:background="@color/color_black_bg">

    <FrameLayout
        android:id="@+id/fm_main_index"
        style="@style/MatchMatch.Vertical"
        android:layout_above="@+id/include_main" />

    <include
        android:id="@+id/include_main"
        layout="@layout/layout_main_bottom"
        android:layout_width="match_parent"
        android:layout_height="@dimen/spacing_maxplus"
        android:layout_alignParentBottom="true" />

</RelativeLayout>
